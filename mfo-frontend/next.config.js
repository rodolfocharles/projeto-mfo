    /** @type {import('next').NextConfig} */
    const nextConfig = {
      output: 'standalone',
      // Se você tiver alguma configuração específica, coloque-a aqui.
      // Por exemplo, se você tinha algo como:
      // experimental: {
      //   serverActions: true,
      // },
      // mantenha apenas o objeto de configuração.
    };

    module.exports = nextConfig;
